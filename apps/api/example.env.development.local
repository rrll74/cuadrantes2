# .env.development.local -> for develope environment
# .env.production.local -> for production use

# Old Database Configuration (mysql)
DB_OLD_HOST=localhost
DB_OLD_PORT=3305
DB_OLD_USERNAME=user
DB_OLD_PASSWORD=password
DB_OLD_DATABASE=db

# New Database Configuration (mariadb)
DB_NEW_HOST=localhost
DB_NEW_PORT=3307
DB_NEW_USERNAME=root
DB_NEW_PASSWORD=password
DB_NEW_DATABASE=db2

# Server configuration
HOST=localhost
PORT=3101
GESTION_PORT=3002
CORS_ALLOWED_ORIGIN=http://10.1.1.160,http://172.,http://192.168.

#App Configuration
JWT_SECRET=ESTE_ES_UN_SECRETO_MUY_LARGO_Y_SEGURO_CAMBIAME
JWT_EXPIRATION=3600s # 1 hora

# Email Configuration (SMTP)
# Deja estos valores vacíos si no quieres enviar emails
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=tu-email@gmail.com
SMTP_PASSWORD=tu-contraseña-app
SMTP_FROM="Cuadrantes <tu-email@gmail.com>"