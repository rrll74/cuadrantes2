// jest.setup.ts
// Este archivo se ejecuta automáticamente antes de cada test.

// Importa los matchers personalizados de jest-dom (ej: .toBeInTheDocument())
// para que estén disponibles en todos los archivos de test.
import "@testing-library/jest-dom";
