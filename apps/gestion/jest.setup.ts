// jest.setup.ts

// Importa los matchers personalizados de jest-dom (ej: .toBeInTheDocument())
// para que estén disponibles en todos los ficheros de test.
// Esto resuelve los errores de TypeScript y configura Jest.
import "@testing-library/jest-dom";
